# 深度研讨思维协议

**用途**：指导 dsh / trae 等 agent 与外部模型（GPT）做**多轮**深度沟通时，怎么提问、用哪套思维、
什么时候该停、什么必须落成状态。

**为什么需要它**：一问一答时模型只能给出"通用正确但无用"的答案。有价值的是围绕同一问题做
**2-4 轮**沟通，而轮次本身不产生深度——**产生深度的是每一轮的 Decision Delta**。

> 结论来自三轮与 GPT 的协议压测（2026-09-20），原始记录见
> `tasks/20260920-thinking-protocol/answer-*.md`。方法论来源：用户个人库
> `~/.codex/skills/personal-thinking-tools`。

## 0｜三个 Gate 构成最小闭环

只保留三个 Gate（其余都是它们的细节）：

| Gate | 判断 | 不过的后果 |
|---|---|---|
| **G1 Problem Validity** | 问题、Authority、关键事实是否足够进入判断？ | 越深入越可能是在解决**错问题** |
| **G2 Decision Delta** | 本轮是否真实改变了决策状态？ | 多轮协议迅速退化成"继续聊" |
| **G3 Commit** | 候选是否满足 Acceptance，能否成为后续依赖的事实/决定/边界？ | 中间推测**污染后续所有阶段** |

顺序固定：**能不能开始 → 值不值得继续 → 能不能生效**。

## 1｜轮次协议（预算是先验，Delta 决定实际轮数）

### R1 诊断轮
输出：真正要解决的决策 + **≤3 个会改变结论**的歧义/隐含假设 + 信息是否足够。

**可以跳过**，当且仅当三条同时满足：Action 明确（要判断/选择/设计/解释什么）、Scope 明确
（对象/范围/时间尺度无歧义）、Optimization Target 基本明确，**且**
"最不确定的假设取反后结论不变"。

⚠️ 取反判据的三个已知漏洞，必须一并检查：

1. **多假设联动**：单变量取反不变，但 A+B 同时变化会翻转（交互项）；
2. **尾部风险**：推荐不变，但小概率不可逆损失会让**执行策略**必须改变；
3. **粒度太粗**："还是选 A"没变，但"全量上线"→"先灰度"已是重大变化。

→ 所以真正要看的不是"推荐是否翻转"，而是
**不确定性 × 决策敏感性**，并覆盖 **Decision Boundary 与执行方式是否变化**。

高风险时**压缩** R1（"我先按 X 为核心目标、Y 为硬约束判断"），而不是删除。

**G1 出口**：
- 事实型未知 → 可澄清、可验证、可显式设为假设；
- **Authority-bound 未知 → 不得用假设替代**，只能 `ask user` / `User Choice Required`
  （例如"增长和利润冲突时你更看重哪个"是用户的价值权，不能替他假设）；
- 原问题其实是下游问题、且存在更上游问题会改变整个讨论 → **重写问题**。

### R2 分叉轮
给 2-3 条**本质不同**的路径；实际只有一条合理路径时**不硬造选项**。
每条只写五项：成立前提 → 主要收益 → 核心代价 → **失败条件** → 最值得验证的变量。

重点不是优缺点全集，而是：**A 与 B 到底在哪个变量上分出胜负。**

**G2 出口**：新方案**具有改变当前选择的现实可能性，且关键依据不足**才进入定向验证；
否则继续收敛。

### R3 判定轮
输出：推荐 **或 Decision Boundary** + 1-3 条关键理由 + 为什么排除其他方案 +
什么条件变化会推翻当前判断 + 是否存在必须用户决定的不可逆/价值取舍。

**取舍标准不足时，不假装推荐**——此时正确输出是"分界线在哪"。

### R4 冲突轮（默认不开）
进入条件（全部满足）：关键事实基本明确、主要方案基本明确、继续补信息无法解决、
剩余困难来自**目标/价值/时间尺度**的真实冲突。

本轮只确认：冲突在哪、哪几个价值无法同时最大化、
**需要接受的代价、边界或持续张力是什么**。

> 不要写死"最终必须牺牲什么"——有些冲突的正确解是分阶段满足、设置阈值、
> 改变问题结构，或接受张力长期存在。这正是六维方法论"允许无解"的定位。

**R4 不预设具体方法论**（见第 2 节 Router）。

## 2｜方法论 Router：什么信号配什么思维

**默认 NONE**——不用任何方法论是默认，不是例外。
以下情况直接 NONE：目标明确、有客观答案、已有成熟规则可直接执行、只是事实查询/局部修改/实现动作、
**或方法论不会改变后续行动**。

信号各取 `0/1/2`（0=无、1=存在但非主导、2=主导问题）：

| 信号 | 含义 | 典型判断 |
|---|---|---|
| **F** | 事实/证据充分性：结论是否高度依赖未验证的外部事实或证据质量 | "数据还没查清就想下结论" |
| **V** | 价值/目标冲突：补事实无法消除 | "都对，但不能同时满足" |
| **E** | 萃取需求：输入主要是案例/经验/流程/失败记录 | "看了这些案例，到底能沉淀什么规则？" |
| **P** | 提示词杠杆：问题可能靠改提示词或调用结构显著改善 | 输出不稳、规则失效、上下文组织问题 |
| **A** | Authority 缺口：关键答案只能由用户提供 | 偏好、价值取舍、业务硬约束未知 |
| **M** | 机制缺口：知道现象但不知道为什么 | "有效，但不知道为什么有效" |
| **X** | 迁移需求：结果要跨执行者/跨轮/跨版本继续使用 | 后续 agent 要依赖当前结论 |

### 路由规则（离散 + 布尔，**禁止加权总分**）

```
F=2                          → 前置阻断：暂停 M1/M2/M3
                               先 verify / research / experiment
                               更新事实后重新 Router
V=2                          → M1 六维深度思考（前提：事实缺口不是主要障碍）
E=2 或 (M=2 且有案例证据)     → M2 萃取方法论
P=2                          → 先过 Prompt-Solvability Gate
                               真属于提示词/上下文/调用结构 → M3
                               否则 NONE 或返回对应流程修复
A=2                          → 不路由方法论；启用 M4 追问技法
                               补齐 Authority 后重新 Router
多个信号同时为 2              → 禁止揉成一个"大方法论"
                               按依赖顺序拆阶段，逐阶段路由
```

**F 为什么是前置阻断**：没有它会有两个典型错误——
"**垃圾进 M2**"（从幸存者样本里萃取出漂亮方法论）和
"**过早进 M1**"（本来是事实没搞清，却被解释成价值冲突）。
Router 能判断"怎么想"，但必须先判断"**现在到底适不适合开始想**"。

### 四套方法论定位

| | 方法论 | 定位 |
|---|---|---|
| M1 | 六维深度思考（哲学） | 六步：问题抱持→要素观照（1 主矛盾+2 关键变量）→意义穿透→边界拓维→矛盾共生→知行安顿。**落点是认知清晰，允许无解** |
| M2 | 萃取方法论 | 从案例提炼可迁移机制。区分目标/事实/机制/规则/参数/任务公式。**禁止无依据的权重与阈值** |
| M3 | 母提示词/提示词架构 | 任务求解优先：先判断"是否真该靠改提示词解决"；机制优先、参数化、强规则弱模板 |
| M4 | 苏格拉底式追问 | **不是独立方法论，是 Question Operator**：不给答案只提问。它没有自己的 Commit 内容，只产出让他人能 Commit 的输入 |

### 互斥与组合

- **一条 Atomic Action 只允许一个 Primary Method。**
- M1 与 M2 不冲突，但先后由依赖决定：事实/机制未清 → `M2 → M1`；
  事实已足够、卡在价值矛盾 → 直接 M1；只要可迁移规则 → M2 完成后结束，**不为了"深度"追加 M1**。
- **禁止 M1+M3 同步**：一个允许"清晰但无解"，一个面向任务求解，**Acceptance 天然冲突**。
- 需要组合时**拆阶段并分别过 Gate**。

## 3｜Decision Delta：判断一轮是否有效

一轮有效必须至少改变一项：

- 新增/修正关键约束；
- 暴露能改变结论的假设；
- 新增或淘汰可行路径；
- 找到新失败模式；
- 明确取舍标准或阈值；
- 确认某未知事实值得验证；
- 推荐变化，或推荐成立的**边界变清楚**。

**且该新增必须改变下一轮的问题、选项或判断。**

### Counterfactual Delta Gate（硬测试）

> **如果本轮新增的内容被证明为假，当前选择或下一步验证会不会改变？**
> 不会 → **No Delta**。"新"不等于"有决策价值"。

典型伪增量：低概率失败模式、边缘约束、"未来可能扩展"的假设——
它们能声称"新增了失败模式"，但既不改变主决策边界，也没有新证据。

### 五种空转信号

1. 结论没变，边界也没变（只是增加论据）；
2. 新问题的答案不会改变行动（**伪追问**，如"你更喜欢简单还是灵活？"而两个方案都满足）；
3. 抽象层级越来越高（系统、长期主义、本质），但仍不知道选什么；
4. **重新打开已关闭的问题**（状态丢失）；
5. 信息变多，但候选空间没有减少（发散失控）。

→ 出现 No Delta：**要么收敛，要么只追问一个最高 Decision Value 的未知**。绝不为了凑轮数继续。

## 4｜停止状态（五选一，必须如实标记）

| 状态 | 含义 |
|---|---|
| `Decision Ready` | 剩余未知不足以改变当前判断 |
| `Decision Boundary Ready` | 不能唯一选择，但知道什么条件下选 A、什么条件下选 B |
| `Execution Boundary` | 剩余问题要通过执行/实验/外部事实回答，继续语言讨论价值低 |
| `User Choice Required` | 剩余是不可替代的个人价值取舍或不可逆决策，推理无法消除 |
| `Plateau` | **未解决但继续讨论收益低** |

⚠️ `Plateau` **不得伪装成收敛**，必须显式标记"尚未解决，唯一还有决策价值的问题是 X，需要验证 X"。

## 5｜提问句式（直接可用，不要写"请进一步说明"）

**R2 用**：

| 句式 | 针对的失效模式 |
|---|---|
| "如果我们现在默认的 X 恰好是错的，结论会不会改变？会怎么改变？" | 隐藏假设 |
| "什么条件一出现，这个方案就可以直接排除？" | 无穷优缺点罗列（逼讨论从"评价"进入"判断"） |
| "**A 和 B 真正决定胜负的那个变量是什么？我们现在知道它吗？**" | 比较维度太多（**最高价值的一句**） |
| "选错 A 和错过 A，哪个代价更大？具体损失是什么？" | 只比收益不比错误成本（尤其适合高影响问题） |
| "我们是不是把问题误写成 A/B 二选一？有没有一条路径能避开两边最昂贵的代价？" | 伪二元选择（**只在双方都有结构性缺陷时用**） |

**R3 用**：

| 句式 | 针对的失效模式 |
|---|---|
| "哪些条件违反一个就不能选？哪些只是更喜欢？" | 硬约束与软偏好混在一起 |
| "X 到什么程度，你会从 A 切换到 B？" | 模糊的"视情况而定"（把推荐变成 Decision Boundary） |

**M4（补 Authority）**：澄清定义 → 质疑假设 → 切换视角（对照"竞争对手/反对者/另一个学科怎么看"）。

## 6｜跨轮 State：State Promotion Gate

每轮结束只问一句：

> **删除这条信息，会不会改变后续的 Router / 候选判断 / Gate / Commit？**
> 不会 → 本轮 scratch，丢弃；会 → 晋升为 State。

State 只保留四类，且**只保留仍被后续依赖的条目**：

```
confirmed / assumption / eliminated / open
```

这条同时满足两个要求：跨轮不丢失已确认结论（防止重开已关闭的问题），
又不为格式完整去保存全部讨论（聊天记录 ≠ 状态数据库）。

## 7｜PRD 的定位

**PRD 不是方法论，是 Contract 的持久化载体。** 不是"复杂就写 PRD"。

触发（任一）：

- 结果要交给**另一个执行者/模型**继续执行；
- 存在**多阶段**执行，后续阶段必须依赖前面锁定的约束；
- 需要跨会话保持 Inputs / Deliverables / Invariants / Authority / Acceptance；
- 后续要做 Regression、版本比较或重复执行，需要 **Material Version**。

关系：`M1/M2/M3 = 怎么思考`，`PRD = 怎么保存并交付已 Commit 的 Contract`。
M2 给 PRD 提供机制/规则/参数，M3 给 PRD 提供提示词架构与调用规范，**PRD 与它们正交**。

> 单会话、单执行者、一次性判断，**即使很复杂也未必需要 PRD**。

## 8｜与确定性工作流共存

- **Gate 验证的是"认知任务是否完成"，不是"问题是否解决"。**
  M1 的 Acceptance = 核心矛盾已明确 + 关键价值与假设已显性化 +
  知道哪些部分无法靠继续推理消除 + 得到 Decision Boundary，或进入
  `end: unresolved-but-clarified`。
- **业务判断阈值 vs 控制流判据**：禁止无证据就声称"成本占 30%、质量占 40%"；
  但允许"V 是否达到主导问题"这类**分类标签**。阈值将来从真实回放数据学习，
  **不现在拍脑袋制造 `0.35V + 0.25E`**。
- 因此 Router **禁止加权总分**，只用离散信号 + 布尔组合。

## 9｜未校准 / 未验证（诚实清单）

- 信号集合（F/V/E/P/A/M/X）与路由规则来自**设计推理 + 三轮压测**，
  **尚未用真实任务回放校准**。
- 校准方法：取过去 30 个真实任务人工标注，让 Router 跑一遍，看三项——
  **是否过度调用方法论 / 是否漏掉明显该用方法论的任务 / 不同人重复标注是否基本一致**。
- 三个 Gate 的**执行成本**未实测（可能对小任务过重）；跳过条件已有，但"跳过是否正确"未统计。
- 若发现某条规则长期不改变行动 → 按"可删除的进化才是进化"删除它，不要只加规则。
